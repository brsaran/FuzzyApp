#!/usr/bin/perl -w

######Required Modules

use List::Util qw[max];
use CGI qw(:standard);
use CGI::Carp qw(fatalsToBrowser);
use MIME::Lite; #To be installed

#########
print header;

print start_html(-head=>meta({-http_equiv => 'Refresh', -content=> '5'; URL=http://localhost/fuzzyapp/index.php'}));

print qq{<h1>Refresh damn you.</h1>};
   
print end_html;
